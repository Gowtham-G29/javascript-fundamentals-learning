// var arr=[12,'ferrari',true,12.4]

// console.log(arr)

// //accessing the elements

// var a=arr[1]
// console.log(a)

// //replace the elements of the array using index

// arr[1]='bently'
// console.log(arr)

// //length property to a identify the length of the array

// console.log('the length of the array is:',arr.length)




//inbuilt js array method

var arr2 = [12,14,56,57]

arr2.pop() //pop method remove the last element from the array
console.log("pop array:",arr2) 

arr2.push(100) // push method will push the element in the end of the array
console.log('push the array:',arr2)

arr2.shift()
console.log("shift the array:",arr2)// shift method that allows to remove the first or starting element of the array

arr2.unshift(102) // unshift method is add the element in the start of the array
console.log("unshifted array:",arr2)
