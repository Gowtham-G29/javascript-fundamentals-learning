function person(_name,_age){
    this.name = _name
    this.age =_age
    this.getName=()=>{
        return name

    }
}

let person1 =new person ("Adam",25)
console.log(person1)
console.log(person1.getName)

// encapsulation means we cannot change the values inside the class from outside


