

console.log(`Scaler is 
Awasome`)                // ` backtick is used to write a strings in multiple line string


let a= 26

console.log(`my age is `+ a)
console.log(`my age is ${a}`) // both are makes the same instead of add the variable value after the quote we can use the $ symbol use as well.
